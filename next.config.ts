/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['utfs.io'],
  },
}

export default nextConfig


// domains: ['osiyjeaifa.ufs.sh'],

// export default nextConfig;
